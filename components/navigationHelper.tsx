import BottomTab from '../components/bottomTab';
import Home from '../interfaces/home';

export const BottomTabComponent = () => <BottomTab />;
export const HomeComponent = (props: any) => <Home {...props} />;
